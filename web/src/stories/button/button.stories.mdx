import {
  Meta,
  Story,
  Canvas,
  Preview,
  Props,
} from '@storybook/addon-docs/blocks'
import { Button } from '../../components/button'
import { Icon } from '../../components/icon'

# Button

With `MDX` we can define a story for `Button` right in the middle of our
markdown documentation.

export const Template = args => <Button {...args} />

<Meta
  title="Button"
  component={Button}
  parameters={{
    design: {
      type: 'figma',
      url:
        'https://www.figma.com/file/Mc03kFcaFlcMOA7afjaZmF/Grape-DS-Components-Web?node-id=1%3A2',
    },
  }}
/>

## Primary Button

<Canvas>
  <Story
    name="base"
    args={{
      type: 'primary',
      children: 'Primary',
      onClick: () => alert('Button pressed'),
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="primary-minimal"
    args={{
      type: 'primary',
      style: 'minimal',
      children: 'Primary',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Basic Button

<Canvas>
  <Button type="basic">Basic</Button>
  <Button type="basic" style="minimal">
    Basic
  </Button>
  <Button type="basic" size="small">
    Basic
  </Button>
</Canvas>

## Danger Button

<Canvas>
  <Button type="danger">Danger</Button>
  <Button type="danger" style="minimal">
    Danger
  </Button>
  <Button type="danger" size="small">
    Danger
  </Button>
</Canvas>

## Icon Button

<Canvas>
  <Button type="primary" style="standard" size="regular" icon="close" />
  <Button type="primary" style="standard" size="regular" icon="people" />
</Canvas>

## Icon Button

<Canvas>
  <Button type="primary" style="standard" size="regular" icon="close">
    Left
  </Button>
  <Button
    type="primary"
    style="standard"
    size="regular"
    icon="close"
    iconPosition="right"
  >
    Right
  </Button>
</Canvas>
