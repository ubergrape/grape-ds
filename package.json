{
  "name": "aurora",
  "version": "1.0.0",
  "repository": "https://github.com/ubergrape/grape-ds.git",
  "author": "UberGrape Gmbh <support@grape.io>",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "web",
    "test-aurora",
    "docs"
  ],
  "scripts": {
    "dev": "yarn --cwd web dev",
    "build:storybook": "yarn --cwd web build",
    "build:docs": "yarn --cwd docs build",
    "build": "yarn --cwd web build && mkdir -p build/docs && cp _redirects build/_redirects",
    "clean": "rm -rf build && rm -rf docs/node_modules && rm -rf web/node_modules"
  }
}
